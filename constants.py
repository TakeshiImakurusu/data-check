"""
定数定義モジュール

各シリーズモジュールで使用されるマジックナンバーを定数として定義します。
コードの可読性と保守性を向上させるために使用します。
"""

from enum import Enum, IntEnum


# =============================================================================
# 販売店コード定数
# =============================================================================

class DealerCode:
    """販売店コード定数"""
    
    # リコー
    RICOH = "004359"
    
    # 建築資料研究社
    KENCHIKU_SHIRYO = "000286"
    
    # キヤノン（新潟のみ）
    CANON_NIIGATA = "001275"
    
    # ITS三島
    ITS_MISHIMA = "000332"
    
    # ITS札幌
    ITS_SAPPORO = "A30777"
    
    # 富士ビジネス
    FUJI_BUSINESS = "000583"
    
    # 富士FBI秋田
    FUJI_FBI_AKITA = "000659"
    
    # 精密舎
    SEIMITUSHA = "000759"
    
    # 旭測器
    ASAHI_SOKKI = "B88299"
    
    # 建設システム本社直販
    KSHH = "kshh"
    
    # 全販売店（無効な値として使用されることがある）
    KS_ALL = "ksALL"


# =============================================================================
# 入金経路コード定数
# =============================================================================

class PaymentRoute:
    """入金経路（Nsyu）コード定数"""
    
    # 自動振替（トータルネット）
    AUTO_TRANSFER_TOTALNET = "121"
    
    # 自動振替（販売店経由）
    AUTO_TRANSFER_DEALER = "122"
    
    # 更新案内不要
    NO_RENEWAL_NOTICE = "112"
    
    # 銀行振込
    BANK_TRANSFER = "211"
    
    # 整数版（CLOUDシリーズ等で使用）
    class Int(IntEnum):
        AUTO_TRANSFER_TOTALNET = 121
        AUTO_TRANSFER_DEALER = 122
        NO_RENEWAL_NOTICE = 112
        BANK_TRANSFER = 211


# =============================================================================
# 更新案内フラグ定数
# =============================================================================

class UpdateNoticeType(IntEnum):
    """更新案内送付フラグ"""
    
    # 不要
    NOT_REQUIRED = 0
    
    # 送る（新規）
    SEND_NEW = 1
    
    # 送る（既存）
    SEND_EXISTING = 2


# =============================================================================
# CLOUDシリーズ更新制限定数
# =============================================================================

class CloudUpdateLimit(IntEnum):
    """maiCloudUpdateLimit の値"""
    
    LIMIT_1 = 1
    LIMIT_2 = 2
    LIMIT_3 = 3
    LIMIT_4 = 4
    LIMIT_5 = 5


# =============================================================================
# シリーズ名定数
# =============================================================================

class SeriesName:
    """シリーズ名定数"""
    
    DEKISPART = "DEKISPART"
    INNOSITE = "INNOSiTE"
    DEKISPART_SCHOOL = "DEKISPART_SCHOOL"
    CLOUD = "CLOUD"


# =============================================================================
# エラー重要度定数
# =============================================================================

class Severity:
    """エラー重要度"""
    
    ERROR = "エラー"
    WARNING = "警告"


# =============================================================================
# 無効な販売店マーカー（名前の先頭に付く記号）
# =============================================================================

class InvalidDealerMarker:
    """無効な販売店を示す記号"""
    
    BANKRUPTCY = "★"  # 倒産
    INVALID = "×"     # 無効
    CAUTION = "▲"     # 注意
    STOP = "■"        # 停止
    DOT = "・"        # ドット（担当者用）
    
    # 販売店チェック用リスト
    FORBIDDEN_LEADING_SYMBOLS = ["：", "×", "▲", "★", "■"]
    
    # 担当者チェック用リスト
    SALES_PERSON_INVALID_PREFIXES = ["×", "・"]


# =============================================================================
# 備考キーワード定数
# =============================================================================

class BikoKeyword:
    """備考欄に含まれるキーワード"""
    
    RENEWAL_NOT_NEEDED = "更新案内不要"
    JIFURI_DM_NOT_NEEDED = "自振DM不要"
    SPECIAL_CALCULATION = "特別計算"
    SUBSIDY = "補助金"
    SPECIAL_SHIPPING = "特別発送"
    NP_NOT_ALLOWED = "NP不可"
    REDUCE_AND_RENEW = "減らして更新"
    CANCEL = "退会"
